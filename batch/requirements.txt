# Folder Batch Anonymization System - Python Dependencies
# Install with: pip install -r requirements.txt

# Core document processing libraries (same as main app)
python-docx>=1.1.0      # Word document (.docx) processing
python-pptx>=0.6.21     # PowerPoint (.pptx) processing
openpyxl>=3.1.0         # Excel (.xlsx) processing and report generation

# Note: LibreOffice must be installed separately for:
# - PDF conversion
# - Legacy format conversion (.doc, .xls, .ppt)
#
# Installation instructions:
# - Windows: winget install LibreOffice.LibreOffice
# - Linux/WSL: sudo apt-get install libreoffice
# - macOS: brew install libreoffice
